const images = state => state.breed;
const apiKey = state => state.api_key;
const categories = state => state.categories;
const currentPageView = state => state.pageView;

export { images, apiKey, categories, currentPageView};
